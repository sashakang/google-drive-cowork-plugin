{
  "mcpServers": {
    "google-drive": {
      "command": "bash",
      "args": ["-c", "source ${CLAUDE_PLUGIN_ROOT}/.venv/bin/activate && python3 -m server.main"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
